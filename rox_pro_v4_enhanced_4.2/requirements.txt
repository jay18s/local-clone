# ROX Proven Edge Engine v4.0 Unified
# =====================================
# Core scientific / data
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Technical analysis
ta-lib>=0.4.0; platform_system != "Windows"    # optional TA-Lib
pandas-ta>=0.3.14b                              # pure-Python fallback

# Async / networking
aiohttp>=3.9.0
aiofiles>=23.1.0
requests>=2.31.0

# Websockets (optional — graceful fallback if absent)
websockets>=11.0; extra == "realtime"

# Data sources
yfinance>=0.2.28
nsepy>=0.8; python_version < "3.12"           # NSE India

# Broker APIs (install as needed)
fyers-apiv3>=3.0.0; extra == "fyers"
kiteconnect>=5.0.0; extra == "zerodha"

# Caching / storage
redis>=4.6.0; extra == "redis"
sqlalchemy>=2.0.0; extra == "db"

# Machine learning (ml_pipeline module)
scikit-learn>=1.3.0
joblib>=1.3.0

# Monitoring / alerting
psutil>=5.9.0
colorama>=0.4.6

# Utilities
python-dotenv>=1.0.0
tabulate>=0.9.0
pytz>=2023.3

# Dev / testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
